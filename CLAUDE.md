# BusinessAgent

NottaのBtoB営業推進のための役割別エージェント環境。ユーザーはNotta株式会社のBtoB営業推進担当者。

## このリポジトリの目的

`.claude/agents/` に定義された5つの役割エージェント（marketing-director / sales-director / coo / legal / cto）を使って、営業活動の壁打ち・成果物レビュー・資料作成を行う。

## 運用ルール

- ユーザーとのやり取りは日本語で行う
- 依頼内容が特定の役割の専門領域に該当する場合は、対応するサブエージェントに委譲する
- 複数の役割の視点が必要な依頼は、関係するエージェントを並行起動して意見を統合する
- どの役割にも該当しない一般的な作業（ファイル整理、ドキュメント整形など）はメインのClaudeが直接対応する
- 営業成績レポートの作成依頼は `sales-report` スキル（`.claude/skills/sales-report/`）の手順・集計定義に従う

### 役割の境界（重複しやすい領域の振り分け）

- **値引き・価格**: 個別商談の値引き判断・交換条件 → sales-director。プラン体系・値引きポリシーの設計 → coo。両方に跨る場合は並行起動
- **セキュリティチェックシート**: 技術仕様の設問 → cto。契約・法令の設問 → legal。混在するシートは両方を並行起動し分担を明示
- **契約判断**: 法的リスクの評価 → legal。ビジネス上の受容判断 → coo

### モデル方針

- 判断の重い役割（coo / legal / cto / sales-director）は `model: inherit`（セッションのモデルを継承）
- 定型度の高い役割（marketing-director）は `model: sonnet` でコストと速度を優先
- 変更は各エージェント定義のfrontmatterで行う

## Git運用ルール

- デフォルトブランチは `main`。直接pushせず、作業ブランチ → PR → マージの流れで変更する
- マージ済みの作業ブランチは削除し、放置しない
- `reports/` 配下には実際の顧客・金額データが入りうる。push前に含めてよいか確認する

## 前提知識

- Nottaは AI音声認識・文字起こしSaaS。多言語対応、Web会議連携（Zoom / Google Meet / Microsoft Teams / Webex）、AI要約が主な機能
- BtoB営業の主戦場は日本市場。稟議文化・セキュリティ審査・比較検討といった日本企業の商習慣を前提とする
- 製品の具体的な仕様・価格は変動するため、断定が必要な場合は「要確認」と明示し、公式情報の確認を促す
